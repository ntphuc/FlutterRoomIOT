import 'package:intl/intl.dart';
class AppConfig {
  static DateFormat dateFormat = DateFormat('EEEE, dd/MM/yyyy');
  static DateFormat dateFormat2 = DateFormat('dd/MM/yyyy, HH:mm');
  static DateFormat timeFormat = DateFormat('HH:mm');
  static DateFormat dayOfWeek = DateFormat('EEEE');
  static const APP_NAME = 'Demo';
}